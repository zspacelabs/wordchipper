"""Drop-in compatibility wrappers for tiktoken and HuggingFace tokenizers.

Usage::

    # Instead of: import tiktoken
    from wordchipper.compat import tiktoken

    # Instead of: from tokenizers import Tokenizer
    from wordchipper.compat.tokenizers import Tokenizer
"""
